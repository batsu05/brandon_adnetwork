<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brandon.brandonadnetworktest" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.GET_ACCOUNTS" />

        <service android:name="com.appnext.appnextsdk.API.DownloadService" />

        <activity
            android:name=".AppnextActivity"
            android:label="@string/title_activity_appnext" >
        </activity>
        <activity
            android:name=".MopubActivity"
            android:label="@string/title_activity_mopub" >
        </activity>
    </application>

</manifest>
